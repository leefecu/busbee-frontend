var React = require('react');

var Header = React.createClass({
    render: function() {
        return (
            <header>
                I'm header
            </header>
        );
    }
    
});
    
module.exports = Header;
