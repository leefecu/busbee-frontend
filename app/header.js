var React = require('react');

var Header = React.createClass({
    render: function() {
        return (
            <header>
                Im header
            </header>
        );
    }
    
});
    
module.exports = Header;
